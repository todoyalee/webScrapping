# Browser -> Laravel API (must be reachable from the user's browser).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-side rendering -> Laravel API. Inside docker-compose this is the
# backend service name; left unset it falls back to NEXT_PUBLIC_API_BASE_URL's default.
API_BASE_URL=http://localhost:8000
